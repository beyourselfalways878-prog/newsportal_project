import React from 'react'; import {   Dialog,   DialogContent,   DialogHeader,   DialogTitle,   DialogDescription, } from '@/components/ui/dialog'; import { Button } from '@/components/ui/button'; import { Input } from '@/components/ui/input'; import { useToast } from '@/components/ui/use-toast'; import { Mail, MessageCircle, Facebook, Send, Link as LinkIcon, Check } from 'lucide-react';  const ShareModal = ({ isOpen, setIsOpen, url, title, currentContent }) => {   const { toast } = useToast();   const [copied, setCopied] = React.useState(false);    const shareOptions = [     {       name: 'WhatsApp',       icon: <MessageCircle className="h-6 w-6 text-green-500" />,       href: `https://api.whatsapp.com/send?text=${encodeURIComponent(title + '\n' + url)}`,     },     {       name: 'Facebook',       icon: <Facebook className="h-6 w-6 text-blue-600" />,       href: `https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(url)}`,     },     {       name: 'Telegram',       icon: <Send className="h-6 w-6 text-sky-500" />,       href: `https://t.me/share/url?url=${encodeURIComponent(url)}&text=${encodeURIComponent(title)}`,     },     {       name: 'Email',       icon: <Mail className="h-6 w-6 text-gray-500" />,       href: `mailto:?subject=${encodeURIComponent(title)}&body=${encodeURIComponent('Check out this article: ' + url)}`,     },   ];    const handleCopy = () => {     navigator.clipboard.writeText(url).then(() => {       setCopied(true);       toast({         title: 'Link Copied!',         description: 'The article link has been copied to your clipboard.',       });       setTimeout(() => setCopied(false), 2000);     });   };    return (     <Dialog open={isOpen} onOpenChange={setIsOpen}>       <DialogContent className="sm:max-w-md">         <DialogHeader>           <DialogTitle>{currentContent.shareArticle}</DialogTitle>           <DialogDescription>             {currentContent.shareDescription || 'Share this article with your friends and family.'}           </DialogDescription>         </DialogHeader>         <div className="grid grid-cols-2 sm:grid-cols-4 gap-4 py-4">           {shareOptions.map((option) => (             <a               key={option.name}               href={option.href}               target="_blank"               rel="noopener noreferrer"               className="flex flex-col items-center justify-center space-y-2 p-3 rounded-lg bg-muted hover:bg-muted/80 transition-colors"             >               {option.icon}               <span className="text-xs font-medium text-muted-foreground">{option.name}</span>             </a>           ))}         </div>         <div className="flex items-center space-x-2">           <Input id="link" value={url} readOnly className="flex-1" />           <Button type="button" size="icon" onClick={handleCopy}>             {copied ? <Check className="h-4 w-4" /> : <LinkIcon className="h-4 w-4" />}             <span className="sr-only">Copy Link</span>           </Button>         </div>       </DialogContent>     </Dialog>   ); };  export default ShareModal;


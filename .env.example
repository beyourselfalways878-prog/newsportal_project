# Example environment variables (do NOT commit real secrets)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
# Service role key is sensitive — store it in your hosting provider / CI secrets and never commit to the repo
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# Optional: CREATE_PROFILE_SECRET for the /api/create-profile endpoint
# CREATE_PROFILE_SECRET=your-secret
